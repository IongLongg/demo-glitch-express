extends layout

block content
    .container.d-flex.justify-content-center.pt-5
        h1= `Welcome ${user ? user.name : ''} to ${shop ? shop.name : 'Book List'} !`
        if shop 
            a.btn.btn-lg.btn-outline-dark(href=`/shop/${shop.id}`) Profile